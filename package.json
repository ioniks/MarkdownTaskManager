{
  "name": "markdown-task-manager",
  "version": "1.3.2",
  "private": true,
  "type": "module",
  "description": "Single-file Markdown Kanban task manager — built from src/ into one self-contained, dependency-free task-manager.html",
  "scripts": {
    "build": "node build.mjs",
    "build:dev": "node build.mjs --dev"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0"
  }
}
